# @geoql/doctor — Design Spec

> **The single locked spec.** Goals, repo layout, CLI surface, output format, scoring, config, the full rule catalog, and the hybrid multi-pass engine design (§10) all live here. The code migrates to this spec — never the reverse. (The former `docs/ARCHITECTURE.md` alpha snapshot was folded into §10 and removed.)

**Status:** Locked — single source of truth
**Date:** 2026-05-28
**Owner:** Vinayak Kulkarni
**Repo:** [`geoql/doctor`](https://github.com/geoql/doctor)
**Inspiration:** [`millionco/react-doctor`](https://github.com/millionco/react-doctor) — _"Your agent writes bad React. This catches it."_

---

## 1. Goal

Two CLI tools that audit Vue/Nuxt apps for performance, correctness, security, and best-practice violations — with an **AI-agent-readable output format** so Claude Code / Cursor / OpenCode can read findings and immediately act on them.

```bash
npx -y @geoql/vue-doctor      # → score 0-100 + actionable findings
npx -y @geoql/nuxt-doctor     # → same, with Nuxt rules + Vue rules combined
```

Tagline: _"Your agent writes bad Vue/Nuxt. This catches it."_

---

## 2. Non-goals

- ❌ Not a replacement for ESLint/oxlint — complements them with rules too expensive/contextual for general lint.
- ❌ The OSS CLI ships **no remote dependency** — all scoring is **local + deterministic** (same code in → same score out, fully offline). A hosted layer (`the-doctor.report`: score history, PR-trend graphs, team dashboards, CI baseline-gating) is a separate product built on top of this open core; it is out of scope for this repo and never required to run the CLI.
- ❌ Not a fixer-first tool. `--fix` lands in Phase 5, after rules are stable.
- ❌ Not Vue 2 / Nuxt 2 / Nuxt 3 compatible. **Vue 3 + Nuxt 4 only.** No backward-compat shims, no `compatibilityVersion: 3` detection — we assume the new `app/` directory layout, modern data-fetching defaults, and Nuxt 4 module APIs.

---

## 3. Repo layout

```
~/dev/personal/tools/geoql/doctor/
├── .github/
│   ├── workflows/
│   │   ├── pipeline.yml          # orchestrator (workflow_dispatch + reusable)
│   │   ├── ci.yml                # reusable workflow_call
│   │   ├── lint-branch.yml       # reusable
│   │   ├── lint-pr.yml           # reusable
│   │   ├── demo.yml              # PR demo runs
│   │   └── release-please.yml    # npm + JSR publish (your standard)
│   └── actions/
│       ├── vue-doctor/action.yml      # composite GH Action
│       └── nuxt-doctor/action.yml
├── .husky/
│   ├── pre-commit                # pnpm exec lint-staged
│   └── commit-msg                # pnpm exec commitlint --edit $1
├── .nvmrc                         # 24
├── packages/
│   ├── doctor-core/                        # @geoql/doctor-core
│   │   ├── src/
│   │   │   ├── index.ts                    # public API: runDoctor(), defineConfig()
│   │   │   ├── run-diagnostics.ts          # orchestrator: detect → lint (oxlint) → dead-code (knip) → score → report
│   │   │   ├── invoke-oxlint.ts            # programmatic oxlint runner (spawn + JSON parse)
│   │   │   ├── build-oxlint-config.ts      # synthesize per-run oxlintrc from doctor.config
│   │   │   ├── detect-project.ts           # framework (vue/nuxt), version, capabilities
│   │   │   ├── detect-user-lint-config.ts  # respect existing oxlintrc/eslint config (extend, don't override)
│   │   │   ├── check-dead-code.ts          # knip programmatic
│   │   │   ├── calculate-score.ts          # local-only, deterministic √-decay
│   │   │   ├── apply-ignore-overrides.ts   # --respect-inline-disables logic
│   │   │   ├── filter-for-surface.ts       # --pr-comment / --fail-on / tag filtering
│   │   │   ├── git/                        # --diff / --staged / repo metadata
│   │   │   ├── reporters/
│   │   │   │   ├── agent.ts                # DEFAULT — structured for LLM
│   │   │   │   ├── pretty.ts               # --format=pretty
│   │   │   │   ├── json.ts                 # --json
│   │   │   │   ├── sarif.ts                # GitHub Code Scanning
│   │   │   │   ├── annotations.ts          # GitHub Actions ::error::
│   │   │   │   └── markdown.ts             # --format=markdown
│   │   │   ├── types/                      # Diagnostic, ProjectInfo, ScoreResult, Capability
│   │   │   └── config/load.ts              # c12 loader (.ts/.js/.json/.mjs + pkg.json#doctor)
│   │   ├── package.json
│   │   ├── vite.config.ts                  # vp pack
│   │   └── tsconfig.json
│   │
│   ├── oxlint-plugin-vue-doctor/           # @geoql/oxlint-plugin-vue-doctor
│   │   │                                   # — also distributable standalone for vp lint users
│   │   ├── src/
│   │   │   ├── index.ts                    # plugin entry (default export = oxlint plugin)
│   │   │   ├── plugin/
│   │   │   │   ├── plugin.ts               # defines plugin shape
│   │   │   │   ├── rule-registry.ts        # GENERATED by `pnpm gen` (mirrors react-doctor pattern)
│   │   │   │   ├── utils/
│   │   │   │   │   ├── rule.ts             # defineRule() — Rule interface (id, severity, category, framework, requires, disabledBy, tags, defaultEnabled, recommendation, create)
│   │   │   │   │   ├── rule-context.ts
│   │   │   │   │   ├── rule-visitors.ts    # SFC <template> visitors (compiler-sfc) + script visitors (oxc-parser)
│   │   │   │   │   ├── is-vue-sfc.ts
│   │   │   │   │   ├── get-script-setup-block.ts
│   │   │   │   │   └── get-template-block.ts
│   │   │   │   ├── constants/
│   │   │   │   │   ├── reactivity-apis.ts  # ref, reactive, shallowRef, computed, toRefs, etc
│   │   │   │   │   ├── lifecycle-hooks.ts
│   │   │   │   │   └── vue-built-ins.ts
│   │   │   │   └── rules/                  # one file per rule (NOT colocated in CLI pkg)
│   │   │   │       ├── reactivity/         # cross-file context oxlint can't express
│   │   │   │       ├── composition/
│   │   │   │       ├── performance/
│   │   │   │       ├── template-perf/      # ONLY rules eslint-plugin-vue doesn't cover
│   │   │   │       ├── build-quality/
│   │   │   │       └── ai-slop/            # rules specifically catching AI-agent mistakes
│   │   │   ├── test-utils/
│   │   │   │   ├── run-rule.ts
│   │   │   │   └── run-fixtures.ts
│   │   │   └── scripts/generate-rule-registry.mjs   # codegen — scans rules/ tree, emits rule-registry.ts
│   │   ├── tests/                          # vitest, fixtures: good/bad SFCs
│   │   ├── package.json                    # peerDep: oxlint ^1.67
│   │   ├── vite.config.ts
│   │   └── tsconfig.json
│   │
│   ├── oxlint-plugin-nuxt-doctor/          # @geoql/oxlint-plugin-nuxt-doctor
│   │   ├── src/
│   │   │   ├── index.ts                    # plugin entry — extends vue plugin's rules + adds Nuxt rules
│   │   │   ├── plugin/
│   │   │   │   ├── plugin.ts
│   │   │   │   ├── rule-registry.ts        # GENERATED
│   │   │   │   ├── utils/
│   │   │   │   │   ├── is-server-route.ts          # server/api/**, server/routes/**
│   │   │   │   │   ├── is-composable-file.ts       # composables/**, app/composables/**
│   │   │   │   │   ├── is-page.ts                  # pages/**, app/pages/**
│   │   │   │   │   └── is-plugin.ts                # plugins/**, app/plugins/**
│   │   │   │   ├── constants/
│   │   │   │   │   ├── nuxt4-auto-imports.ts       # full auto-import surface
│   │   │   │   │   ├── nitro-presets.ts
│   │   │   │   │   └── nuxt-modules.ts             # recommended + known-bad-on-nuxt-4
│   │   │   │   └── rules/
│   │   │   │       ├── data-fetching/
│   │   │   │       ├── server-routes/
│   │   │   │       ├── seo/
│   │   │   │       ├── hydration/
│   │   │   │       ├── modules-deps/
│   │   │   │       ├── nitro/
│   │   │   │       └── cloudflare/                  # geoql specialty
│   │   │   └── scripts/generate-rule-registry.mjs
│   │   ├── tests/
│   │   ├── package.json                    # peerDep: oxlint ^1.67, dep: @geoql/oxlint-plugin-vue-doctor
│   │   ├── vite.config.ts
│   │   └── tsconfig.json
│   │
│   ├── vue-doctor/                         # @geoql/vue-doctor (CLI binary)
│   │   ├── src/
│   │   │   ├── cli.ts                      # cac entry — parses flags, delegates to doctor-core
│   │   │   ├── bin.ts                      # shebang wrapper, signal handlers
│   │   │   ├── index.ts                    # programmatic API (re-exports doctor-core)
│   │   │   └── presets/
│   │   │       ├── recommended.ts          # enables the doctor-owned rules + selected eslint-plugin-vue rules
│   │   │       ├── strict.ts
│   │   │       └── minimal.ts
│   │   ├── package.json                    # deps: @geoql/doctor-core, @geoql/oxlint-plugin-vue-doctor
│   │   │                                   # bin: { "vue-doctor": "./bin/vue-doctor.js" }
│   │   ├── vite.config.ts
│   │   └── tsconfig.json
│   │
│   └── nuxt-doctor/                        # @geoql/nuxt-doctor (CLI binary)
│       ├── src/
│       │   ├── cli.ts
│       │   ├── bin.ts
│       │   ├── index.ts
│       │   └── presets/
│       │       ├── recommended.ts          # vue + nuxt presets layered
│       │       └── strict.ts
│       ├── package.json                    # deps: @geoql/doctor-core, @geoql/oxlint-plugin-nuxt-doctor
│       │                                   # transitive: oxlint-plugin-vue-doctor
│       │                                   # bin: { "nuxt-doctor": "./bin/nuxt-doctor.js" }
│       ├── vite.config.ts
│       └── tsconfig.json
├── examples/                      # dogfood fixtures
│   ├── vue-bad/                  # intentionally violates every rule (test target)
│   ├── vue-good/                 # clean, target score 100
│   ├── nuxt-bad/
│   └── nuxt-good/
├── docs/                          # docs site (Phase 5)
├── package.json                   # root, private:true, no main
├── pnpm-workspace.yaml            # packages: ['packages/*', 'examples/*']
├── tsconfig.json                  # root, references packages/*/tsconfig.json
├── vite.config.ts                 # root vp config (fmt.ignorePatterns + lint plugins)
├── README.md
├── SPEC.md                        # this file
├── lint-staged.config.js
├── commitlint.config.js
├── .gitignore
└── .editorconfig
```

---

## 4. Stack — locked to your conventions

| Concern                    | Tool                                                                                                         | Reason                                                                |
| -------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| Package mgr                | **pnpm `^11.4.0`** (via corepack)                                                                            | Latest stable — verify with `npm view pnpm version` at scaffold time  |
| Build                      | **vite-plus `^0.1.22`** (`vp pack`)                                                                          | Latest, rolldown under hood                                           |
| Lint                       | **vp lint** (oxlint `^1.67.0` bundled, inline `oxlintrc` in vite.config.ts)                                  | Your standard                                                         |
| Lint engine (runtime)      | **oxlint `^1.67.0`** invoked programmatically by `doctor-core` + **JS plugin API (alpha)** for rule packages | Mirrors react-doctor; piggybacks on oxc-parser (already a doctor dep) |
| Format                     | **vp fmt** (oxfmt)                                                                                           | Your standard                                                         |
| Runtime                    | Node ≥24                                                                                                     | Your standard                                                         |
| Publish                    | npm + JSR via release-please `^17.6.1`                                                                       | Your standard                                                         |
| Commits                    | conventional + Signed-off-by, commitlint `^21.0.1`                                                           | Your standard                                                         |
| Workflows                  | pipeline.yml orchestrator (verbatim Option A from v-maplibre)                                                | Your standard                                                         |
| Husky                      | `^9.1.7` — `pnpm exec lint-staged@^17.0.5`, `pnpm exec commitlint`                                           | Your standard                                                         |
| TypeScript                 | **`^6.0.3`** (typescript-go aka `@typescript/native-preview`), vue-tsc `^3.3.2`                              | Latest                                                                |
| Tests                      | **vitest `^4.1.7`**                                                                                          | Lightest path                                                         |
| CLI parser                 | **cac `^7.0.0`**                                                                                             | 3kb, ESM, simple — **bumped from 6 → 7**                              |
| SFC parsing                | **@vue/compiler-sfc `^3.5.35`**                                                                              | Official Vue parser, pinned to vue runtime                            |
| TS parsing                 | **oxc-parser `^0.133.0`**                                                                                    | aligns with your oxlint usage                                         |
| Config loader              | **c12 `^4.0.0-beta.5`** (unjs) ⚠️ beta — pin loose; fallback to `^3.x` if churny                             | Nuxt-compatible layered config                                        |
| Globbing                   | **tinyglobby `^0.2.16`**                                                                                     | Lightweight, fast                                                     |
| Color                      | **picocolors `^1.1.1`**                                                                                      | 1kb, no deps                                                          |
| Spinner                    | **yocto-spinner `^1.2.0`**                                                                                   | Tiny, ESM                                                             |
| Dead code scan             | **knip `^6.14.2`** programmatic                                                                              | Best-in-class                                                         |
| Dep audit                  | **pkg-types `^2.3.1`** + npm registry lookups                                                                | No `npm outdated` shelling                                            |
| Path matching              | **picomatch `^4.0.4`**                                                                                       | Used by react-doctor; gitignore-style for `--include`/`--exclude`     |
| Prompts (init)             | **prompts `^2.4.2`**                                                                                         | Minimal CLI prompts for `doctor init` subcommand                      |
| Persistent config (CLI)    | **conf `^15.1.0`**                                                                                           | Stores per-user CLI preferences (color, default reporter)             |
| Existing rules to leverage | **eslint-plugin-vue `^10.9.1`** + **@nuxt/eslint `^1.15.2`**                                                 | Loaded via oxlint's eslint-compat layer for non-doctor-owned rules    |

**Maintenance principle**: All version pins above are **point-in-time snapshots** (captured 2026-05-28 via `npm view <pkg> version`). Before scaffolding the repo OR adding any new dep, re-run `npm view` and use the latest stable. Project memory pins (e.g. "pnpm 11.1.2") are advisory only — always verify.

---

## 5. CLI surface

```
USAGE
  npx -y @geoql/vue-doctor [options] [path]

ARGS
  [dir]                    project dir to scan (default: ".")

CORE OPTIONS
  --preset <name>          recommended (default) | strict | minimal | all
  --rule <name>            run only matching rule(s); repeatable; picomatch glob
  --skip-rule <name>       skip matching rule(s); repeatable
  --include <glob>         file glob (default: respects detected src/ and app/)
  --exclude <glob>         file glob to exclude
  --config <path>          path to doctor.config.ts (or .js/.json/.mjs)
  --no-config              skip config file entirely

DIAGNOSTIC SURFACES (toggle layers)
  --lint                   enable oxlint-based rule layer (default)
  --no-lint                skip lint layer
  --dead-code              enable knip dead-code analysis (default)
  --no-dead-code           skip dead-code layer

SCOPING (which files to scan)
  --diff                   scan only files changed vs HEAD (unions non-ignored untracked)
  --staged                 scan only git-index staged files (pre-commit hooks)
  --changed-files-from <ref>  scan only source files changed since <ref> (e.g. origin/main)
                           unresolvable ref exits 2; mutually exclusive with --diff/--staged
  --include-untracked      fold non-ignored untracked files into --staged / --changed-files-from
  --full                   force full scan (overrides all scope flags)
  --project <name>         scan specific workspace project (comma-separated for multiple)

SUPPRESSIONS
  --respect-inline-disables    honor inline `// oxlint-disable*` / `// doctor-disable*` (default)
  --no-respect-inline-disables AUDIT MODE — neutralize suppressions before scanning

OUTPUT FORMAT
  --format <fmt>           agent (default) | pretty | json | sarif | markdown
  --json                   shorthand for --format json
  --json-compact           with --json, emit single-line JSON
  --output <file>          write report to file (default: stdout)
  --verbose                show every rule + per-file detail (default shows top 3)
  --quiet                  only show summary
  --no-color               disable colors

CI BEHAVIOR
  --ci                     enable GH Actions annotations + non-interactive mode (auto-detect)
  --annotations            emit GitHub Actions ::error::/::warning:: lines
  --pr-comment             tune output for sticky PR comments (drops weak-signal families)
  --fail-on <level>        exit non-zero on: error (default) | warning | none
  --threshold <number>     fail if score < threshold (overrides --fail-on)
  -y, --yes                skip prompts (non-interactive)

DIAGNOSIS HELPERS
  --explain <file:line>    diagnose why a rule fired (or why a suppression didn't apply)
  --why <file:line>        alias for --explain
  --score                  output only the numeric score (for piping)
  --no-score               skip score (useful in CI when score isn't desired)

FIX
  --fix                    apply safe auto-fixes (Phase 5; landing rule-by-rule)

META
  -v, --version
  -h, --help

SUBCOMMANDS
  init                     scaffold doctor.config.ts + populate package.json#doctor
  list-rules [--preset]    print all rules + severities + categories
  explain <rule-id>        show full rule docs in terminal
  inspect [dir]            print detected project capabilities (framework, version, modules)

EXIT CODES
  0    score >= threshold, --fail-on level not triggered
  1    score < threshold OR --fail-on level triggered
  2    invalid args / config error
  3    no source files matched (likely misconfigured --include)
```

`@geoql/nuxt-doctor` has the **same surface** — it just loads both vue + nuxt plugin rule sets by default. Flags map 1:1 between react-doctor and our tools (intentional — easier for AI agents to learn one mental model).

---

## 6. Default output (agent-friendly)

```
@geoql/vue-doctor v0.1.0
analyzed 47 files in 1.2s

SCORE: 72/100 (threshold: 0)

FINDINGS: 12 (3 error, 7 warn, 2 info)

[1] error  reactivity/no-destructure-reactive
    file: src/components/UserCard.vue:14:7
    code: const { name, age } = props
    fix:  const { name, age } = toRefs(props)
    why:  Destructuring a reactive object loses reactivity. `toRefs` wraps each
          property in a ref so reactivity survives destructuring.
    docs: https://github.com/vinayakkulkarni/doctor/rules/reactivity/no-destructure-reactive

[2] error  reactivity/no-mutation-in-computed
    file: src/composables/useCart.ts:24:5
    code: const total = computed(() => { cart.value.tax = 0.1; ... })
    fix:  Move side-effect to a watcher; computed should be pure.
    why:  Mutations inside computed cause infinite re-renders + stale reads.
    docs: ...

[3] warn   performance/v-memo-on-large-list
    file: src/pages/UserList.vue:42:5
    code: <UserCard v-for="user in users" :key="user.id" />
    fix:  Add v-memo="[user.id, user.name]"
    why:  Lists with >100 items benefit from v-memo when only some props drive re-renders.
    docs: ...

...

NEXT STEPS:
  • Auto-fix 4 safe findings: npx -y @geoql/vue-doctor --fix
  • Biggest score impact:
      −15 pts  3× reactivity/no-destructure-reactive
      −10 pts  2× reactivity/no-mutation-in-computed
      −8  pts  4× performance/v-memo-on-large-list
  • Run `@geoql/vue-doctor explain reactivity/no-destructure-reactive` for full context.
```

**Why agent-first by default**: each finding is a structured block (`file:`, `code:`, `fix:`, `why:`, `docs:`). An LLM reading this can immediately produce the right edit. A human can read it too, just with less color. `--pretty` gives the colored emoji version for humans who prefer.

---

## 7. Scoring algorithm

Deterministic, local, no network.

```ts
const SEVERITY_WEIGHTS = { error: 5, warn: 2, info: 0.5 };

function score(findings: Finding[]): number {
  // Per-rule scaling cap: repeated occurrences of same rule decay
  // (5×same-rule shouldn't tank score from 100 → 0 trivially)
  const byRule = groupBy(findings, (f) => f.ruleId);
  let penalty = 0;
  for (const [ruleId, list] of byRule) {
    const rule = list[0];
    const weight = rule.config?.weight ?? SEVERITY_WEIGHTS[rule.severity];
    // First occurrence full, subsequent occurrences scaled by sqrt
    for (let i = 0; i < list.length; i++) {
      penalty += weight * (i === 0 ? 1 : 1 / Math.sqrt(i + 1));
    }
  }
  return Math.max(0, Math.round(100 - penalty));
}
```

Weights are overridable per rule in `doctor.config.ts`. The √-scaling prevents one chatty rule from dominating the score, which matters because the **score is the user-facing artifact**.

### 7.1 Per-dimension sub-scores (additive)

The single 0–100 `score` above is unchanged and remains the **authoritative** value. In addition, `ScoreResult` carries `byDimension`: a per-dimension sub-score that lets a consumer see where a project is strong or weak (e.g. a high overall score that hides a poor performance dimension).

Every `RuleCategory` maps to exactly one of six dimensions (a partition):

| Dimension         | Categories                                                                   |
| ----------------- | ---------------------------------------------------------------------------- |
| `performance`     | `performance`, `template-perf`                                               |
| `correctness`     | `reactivity`, `composition`, `template`, `sfc`, `vue-builtin`, `hydration`   |
| `security`        | `security`                                                                   |
| `maintainability` | `ai-slop`, `dead-code`, `build-quality`, `deps`, `structure`, `modules-deps` |
| `nuxt`            | `nitro`, `seo`, `cloudflare`, `server-routes`, `data-fetching`               |
| `design`          | `design`                                                                     |

Each dimension score uses the **same √-decay formula** isolated to that dimension's diagnostics:

```ts
function dimensionScore(diagnosticsInDimension: Finding[]): number {
  const byRule = groupBy(diagnosticsInDimension, (f) => f.ruleId);
  let penalty = 0;
  for (const [, list] of byRule) {
    const weight = list[0].config?.weight ?? SEVERITY_WEIGHTS[list[0].severity];
    for (let i = 0; i < list.length; i++) {
      penalty += weight * (i === 0 ? 1 : 1 / Math.sqrt(i + 1));
    }
  }
  return Math.max(0, Math.round(100 - penalty));
}
```

Because the dimensions partition the categories, each ruleId belongs to exactly one dimension, so the per-dimension **penalties** sum to the overall penalty (before clamping/rounding). The per-dimension **scores** do **not** sum to 100 — `max(0, …)` and `round()` are applied independently per dimension. Sub-scores are diagnostic aids, not components that combine into the overall score. Dimensions with no findings score 100. `byDimension` is additive to `ScoreResult` and to the JSON reporter's `score` object (schema version stays `1`); it does not alter the single-score contract or the external score-API push payload.

---

## 8. Config file

```ts
// doctor.config.ts
import { defineConfig } from '@geoql/doctor-core';

export default defineConfig({
  preset: 'recommended',
  include: ['src/**/*.{vue,ts,tsx}', 'composables/**/*.ts'],
  exclude: ['**/node_modules/**', '**/dist/**', '**/*.d.ts'],
  threshold: 80,
  rules: {
    'reactivity/no-destructure-reactive': 'error',
    'performance/v-memo-on-large-list': ['warn', { minListSize: 100 }],
    'deps/outdated-deps': 'off',
    'seo/og-image-defined': 'warn',
  },
  reporters: ['agent'],
  respectInlineDisables: true, // honor `// doctor-disable rule-name`
});
```

Loaded via `c12` so it can:

- be `.ts`, `.mjs`, `.js`, `.json`, or in `package.json#doctor` (any of these locations work — `c12` discovers in order)
- layer via `extends: ['./presets/strict']` (merges presets)
- watch in dev mode

**Config resolution order** (first match wins):

1. `--config <path>` flag
2. `./doctor.config.ts`
3. `./doctor.config.mjs`
4. `./doctor.config.js`
5. `./doctor.config.json`
6. `"doctor"` key in `./package.json`
7. Built-in `recommended` preset (no config)

---

## 9. Rule set — cross-referenced & owned

**Coverage labels:**

- **`epv-recommended`** — already in eslint-plugin-vue's `vue3-essential` / `vue3-strongly-recommended` / `vue3-recommended` preset. Doctor **enables it** (via oxlint's eslint-compat) but doesn't reimplement.
- **`ne-recommended`** — already in `@nuxt/eslint` default config. Same treatment.
- **`oxlint-core`** — already in oxlint core (e.g. `no-unused-vars`, generic best-practices).
- **`doctor-owned`** — NEW rule, lives in our oxlint plugin packages. The actual reason this project exists.
- **`doctor-augments`** — same rule name as an existing one, but doctor adds cross-file context, capability gating, or score-weighting on top.
- **`advisory`** — can't be linted; surfaced as info-level guidance + link to docs.

### 9.1 vue-doctor — what we LEVERAGE (no reimplementation)

These are already covered by `eslint-plugin-vue@^10.9.1`. Doctor's `recommended` preset enables them via oxlint's eslint-compat. We just include them in the score calculation.

| Rule (eslint-plugin-vue)                            | What it catches                                | doctor severity |
| --------------------------------------------------- | ---------------------------------------------- | --------------- |
| `vue/no-mutating-props`                             | Mutating props in child component              | error           |
| `vue/no-ref-as-operand`                             | Forgetting `.value` on a `ref`                 | error           |
| `vue/no-side-effects-in-computed-properties`        | Side-effects in `computed()`                   | error           |
| `vue/no-use-v-if-with-v-for`                        | `v-if` + `v-for` on same element               | error           |
| `vue/no-async-in-computed-properties`               | Async work inside computed                     | error           |
| `vue/require-v-for-key`                             | Missing `:key` on `v-for`                      | error           |
| `vue/no-template-key`                               | `:key` on `<template>` tag                     | warn            |
| `vue/no-textarea-mustache`                          | Mustache in `<textarea>` (loses reactivity)    | error           |
| `vue/no-v-text-v-html-on-component`                 | `v-html` on components (XSS surface)           | error           |
| `vue/no-export-in-script-setup`                     | Top-level `export` in `<script setup>`         | error           |
| `vue/no-dupe-keys`                                  | Duplicate key names across data/props/computed | error           |
| `vue/no-shared-component-data`                      | Returning shared object from `data()`          | error           |
| `vue/return-in-computed-property`                   | Missing return in computed                     | error           |
| `vue/valid-define-emits` / `vue/valid-define-props` | Malformed defineProps/Emits                    | error           |
| `vue/no-parsing-error`                              | SFC parser errors                              | error           |
| `vue/no-unused-components`                          | Imported component never used                  | warn            |
| `vue/no-unused-vars` (template)                     | `v-for` alias never used                       | warn            |
| `vue/multi-word-component-names`                    | Component names too generic (e.g. `Tab`)       | warn            |
| `vue/require-prop-type-constructor`                 | `prop: 'String'` instead of `prop: String`     | warn            |
| `vue/no-deprecated-*` (entire family)               | Vue 2 patterns                                 | warn            |

→ Doctor's recommended preset turns these on; doctor's rule packages don't reimplement them. **Score reflects findings from these too.** Cost: zero — already shipped, well-tested.

### 9.2 vue-doctor — what we OWN (`@geoql/oxlint-plugin-vue-doctor`)

These rules don't exist anywhere or doctor adds materially more value than existing rules. **~20 rules at v0.1; ~35 by v1.0.**

| Category          | Rule id                                             | Severity | Coverage        | AI-priority | Source                                               |
| ----------------- | --------------------------------------------------- | -------- | --------------- | ----------- | ---------------------------------------------------- |
| **ai-slop**       | `ai-slop/no-destructure-props-without-toRefs`       | error    | doctor-owned    | high        | Vue 3 reactivity guide                               |
|                   | `ai-slop/no-destructure-reactive-without-toRefs`    | error    | doctor-owned    | high        | Vue 3 reactivity guide                               |
|                   | `ai-slop/no-mixed-options-and-composition-api`      | warn     | doctor-owned    | high        | Style guide, observed AI mistake                     |
|                   | `ai-slop/no-non-null-assertion-on-ref-value`        | error    | doctor-owned    | high        | TS misuse pattern                                    |
|                   | `ai-slop/no-imports-from-vue-when-auto-imported`    | warn     | doctor-owned    | high        | Nuxt-specific too (see nuxt-doctor)                  |
| **reactivity**    | `reactivity/watch-without-cleanup`                  | warn     | doctor-owned    | medium      | Vue 3 reactivity guide                               |
|                   | `reactivity/prefer-shallowRef-for-large-data`       | info     | doctor-owned    | medium      | Vue 3 perf guide                                     |
|                   | `reactivity/prefer-readonly-for-injected`           | info     | doctor-augments | low         | augments `provide`/`inject` typing                   |
| **composition**   | `composition/prefer-script-setup-for-new-files`     | warn     | doctor-owned    | medium      | Style guide (priority B)                             |
|                   | `composition/defineProps-typed`                     | warn     | doctor-augments | medium      | augments `vue/valid-define-props` with TS-only check |
| **performance**   | `performance/v-memo-on-large-list`                  | warn     | doctor-owned    | low         | heuristic: `v-for` >100 items, no `v-memo`           |
|                   | `performance/prefer-defineAsyncComponent-on-route`  | info     | doctor-owned    | low         | heuristic: top-level page import                     |
|                   | `performance/no-inline-object-prop-in-list`         | warn     | doctor-owned    | medium      | recreates object every render                        |
| **template-perf** | `template-perf/no-computed-getter-in-template-loop` | warn     | doctor-owned    | medium      | accesses to .value in `v-for`                        |
|                   | `template-perf/avoid-deep-v-bind-spread-in-list`    | info     | doctor-owned    | low         | re-runs reactivity collection                        |
| **build-quality** | `build-quality/tsconfig-strict-required`            | warn     | doctor-owned    | low         | config check (not AST)                               |
|                   | `build-quality/vue-tsc-in-devDeps`                  | warn     | doctor-owned    | low         | config check                                         |
|                   | `build-quality/no-vue-cli`                          | warn     | doctor-owned    | low         | deps check (deprecated)                              |
|                   | `build-quality/eslint-plugin-vue-installed`         | info     | doctor-owned    | low         | deps check                                           |
| **deps**          | `deps/duplicate-vue-versions`                       | error    | doctor-owned    | medium      | cross-package, hard to catch in eslint               |
|                   | `deps/vue-major-current`                            | info     | doctor-owned    | low         | npm registry call                                    |
|                   | `deps/knip-unused-exports`                          | warn     | doctor-owned    | medium      | knip integration                                     |
|                   | `deps/knip-unused-deps`                             | warn     | doctor-owned    | medium      | knip integration                                     |

### 9.3 nuxt-doctor — what we LEVERAGE (no reimplementation)

`@nuxt/eslint`'s default flat config provides 9 buckets — `vue.ts`, `nuxt.ts`, `typescript.ts`, `import.ts`, etc. Doctor turns on the `nuxt.ts` and `vue.ts` rule sets via oxlint compat.

Notable rules from `@nuxt/eslint` we leverage:

- `nuxt/no-multiple-template-root` (Nuxt 4: only single root in `<template>`)
- `nuxt/prefer-import-meta` (replace `process.client`/`.server` with `import.meta.client`/`.server`)
- `nuxt/no-cjs-in-config` (nuxt.config.ts must be ESM)
- All `vue/*` rules from the leverage list above (auto-enabled by Nuxt projects)

### 9.4 nuxt-doctor — what we OWN (`@geoql/oxlint-plugin-nuxt-doctor`) — **Nuxt 4 only**

| Category          | Rule id                                            | Severity | Coverage        | AI-priority | Source                                                                           |
| ----------------- | -------------------------------------------------- | -------- | --------------- | ----------- | -------------------------------------------------------------------------------- |
| **ai-slop**       | `ai-slop/no-process-client-server`                 | error    | doctor-augments | high        | Nuxt 4 → `import.meta.*` (covered by @nuxt/eslint but we bump severity to error) |
|                   | `ai-slop/no-explicit-imports-of-auto-imported`     | warn     | doctor-owned    | high        | catches `import { ref } from 'vue'` when auto-imports enabled                    |
|                   | `ai-slop/no-useState-for-server-data`              | warn     | doctor-owned    | high        | AI pattern: using `useState` where `useFetch`/`useAsyncData` belongs             |
|                   | `ai-slop/no-fetch-in-setup`                        | warn     | doctor-owned    | high        | raw `fetch()` instead of `useFetch` loses SSR + dedup                            |
|                   | `ai-slop/no-mixed-app-and-root-layout`             | warn     | doctor-owned    | medium      | Nuxt 4 supports both `app/` and root layout; mixing them breaks resolution       |
| **structure**     | `structure/uses-app-directory`                     | warn     | doctor-owned    | medium      | Nuxt 4 default; flags legacy flat layout in new projects                         |
|                   | `structure/nuxt-major-current`                     | info     | doctor-owned    | low         | deps check                                                                       |
| **data-fetching** | `data-fetching/useAsyncData-key-required-in-loop`  | error    | doctor-owned    | high        | call-site-aware: only when called dynamically                                    |
|                   | `data-fetching/no-shared-key-across-pages`         | warn     | doctor-owned    | medium      | cross-file analysis — same key in 2+ pages                                       |
|                   | `data-fetching/ssr-safe-onMounted-only-for-client` | warn     | doctor-owned    | medium      | accessing browser APIs in setup                                                  |
| **server-routes** | `server-routes/defineEventHandler-typed`           | warn     | doctor-owned    | medium      | type-check for `H3Event` generics                                                |
|                   | `server-routes/validate-body-with-h3-v2`           | warn     | doctor-owned    | medium      | Nuxt 4 ships h3 v2 — `readValidatedBody` pattern                                 |
|                   | `server-routes/createError-on-failure`             | warn     | doctor-owned    | medium      | unhandled throws leak stack traces                                               |
| **seo**           | `seo/useSeoMeta-on-public-page`                    | warn     | doctor-owned    | medium      | pages/ files w/o useSeoMeta                                                      |
|                   | `seo/og-image-defined`                             | warn     | doctor-owned    | low         | meta property check                                                              |
|                   | `seo/lang-on-html`                                 | warn     | doctor-owned    | low         | nuxt.config app head check                                                       |
| **hydration**     | `hydration/clientOnly-for-browser-apis`            | error    | doctor-owned    | high        | `window`/`document` reference in non-client-only                                 |
|                   | `hydration/no-document-in-setup`                   | error    | doctor-owned    | high        | direct `document` access in setup                                                |
| **modules-deps**  | `modules-deps/no-modules-incompatible-with-nuxt-4` | warn     | doctor-owned    | medium      | known list of broken-on-4 modules                                                |
|                   | `modules-deps/recommended-modules-installed`       | info     | doctor-owned    | low         | suggests @nuxt/image, @nuxt/icon, etc.                                           |
| **nitro**         | `nitro/preset-defined-for-deploy-target`           | warn     | doctor-owned    | medium      | infers from CI / runtime env                                                     |
|                   | `nitro/compatibilityDate-set`                      | error    | doctor-owned    | high        | Nuxt 4 requires this                                                             |
|                   | `nitro/runtime-config-typed`                       | info     | doctor-owned    | low         | NuxtConfig type augmentation                                                     |
| **cloudflare**    | `cloudflare/nitro-cloudflare-preset`               | warn     | doctor-owned    | low         | when wrangler.toml present                                                       |
|                   | `cloudflare/og-image-via-satori`                   | info     | doctor-owned    | low         | nuxt-seo skill guidance                                                          |
|                   | `cloudflare/no-node-only-modules`                  | warn     | doctor-owned    | medium      | flags `node:fs` etc. without Cloudflare compat                                   |

### 9.5 Advisory-only (can't lint, surfaced as info)

| Topic | Surfaced as | Source |
| ----------------------------------------- | ----------------------------------------------- | ------------------------------------------- | --------- |
| Accessibility (semantic HTML, ARIA roles) | info ref to `eslint-plugin-vuejs-accessibility` | community plugin (not yet ported to oxlint) |
| Security (CSP, sanitization) | info ref + link to Vue Security docs | Vue Security guide |
| Production deployment checklist | info ref to docs/CHECKLIST.md | Vue Production guide |
| Bundle analysis | info: "Run `vp build --analyze`" | vp own tooling |
| **typescript** | tsconfig-strict | warn | config |
| | vue-tsc-installed | warn | deps |
| | defineProps-generic-typed | info | AST |
| **deps** | vue-version-supported | error | deps |
| | duplicate-vue-detected | error | deps |
| | unused-deps-knip | warn | dead-code |
| | outdated-major-deps | info | deps |
| **build** | vite-config-present | warn | config |
| | eslint-plugin-vue-installed | info | deps |
| | no-deprecated-vue-cli | warn | deps |

### nuxt-doctor (~40 rules, _extends_ vue-doctor by default) — **Nuxt 4 only**

| Category | Rule | Severity |
| ----------------- | ------------------------------------ | -------- | ----------------------------------------- |
| **structure** | uses-app-directory | error | (Nuxt 4 default; flag legacy root layout) |
| | nuxt-version-4-required | error |
| | compatibilityVersion-4-set | warn |
| **data-fetching** | prefer-useFetch-over-raw-fetch | warn |
| | useAsyncData-requires-key | error |
| | no-shared-key-across-pages | warn | (Nuxt 4 stricter key scoping) |
| | ssr-safe-async-cleanup | warn |
| **rendering** | routeRules-defined-for-pages | info |
| | prerender-config-present-for-static | warn |
| | no-client-only-in-ssr-route | warn |
| **server-routes** | defineEventHandler-typed | warn |
| | validate-body-with-h3-v2 | warn | (Nuxt 4 ships h3 v2) |
| | createError-on-failure | warn |
| **auto-imports** | no-explicit-imports-of-auto-imported | info |
| | imports.dirs-configured-for-utils | info |
| **seo** | useSeoMeta-on-pages | warn |
| | og-image-defined | warn |
| **images** | nuxt-image-installed | info |
| | no-raw-img-tag | warn |
| **hydration** | clientOnly-for-browser-apis | error |
| | no-document-in-setup | error |
| **modules** | recommended-modules-installed | info |
| | no-deprecated-nuxt-3-modules | warn | (modules without Nuxt 4 compat) |
| **nitro** | preset-defined-for-deploy-target | warn |
| | compatibilityDate-set | error |
| | runtime-config-typed | info |
| **cloudflare** | satori-og-image-for-cf | info |
| | nitro-cloudflare-preset | warn |

(Full list in `packages/{vue,nuxt}-doctor/RULES.md` once written.)

---

## 10. Engine strategy — oxlint plugin architecture

**Locked in**, mirrors react-doctor's proven design.

```
                  ┌───────────────────────────┐
   user runs   →  │  vue-doctor / nuxt-doctor │   (cac CLI, thin)
   `npx -y @…`    └──────────────┬────────────┘
                                 ↓
                  ┌───────────────────────────┐
                  │      @geoql/doctor-core   │
                  │  - detect project         │
                  │  - synthesize oxlint cfg  │
                  │  - spawn oxlint           │
                  │  - run knip (dead-code)   │
                  │  - aggregate diagnostics  │
                  │  - calculate score        │
                  │  - emit reporters         │
                  └──────────────┬────────────┘
                                 ↓
                  ┌───────────────────────────┐
                  │  oxlint (^1.67, alpha JS  │  ← invoked as a subprocess
                  │  plugin runtime)          │     via `oxlint --format=json`
                  └──────┬───────────────┬────┘
                         ↓               ↓
       ┌────────────────────┐   ┌────────────────────┐
       │ oxlint-plugin-     │   │ oxlint-plugin-     │
       │ vue-doctor (ours)  │   │ nuxt-doctor (ours) │
       └────────────────────┘   └────────────────────┘
                         ↓               ↓
                        +eslint-plugin-vue (via oxlint eslint-compat)
                        +@nuxt/eslint     (via oxlint eslint-compat)
                        +eslint-plugin-react-hooks-style (none for Vue, advisory)
```

### Why this is the only sane choice (vs. from-scratch AST)

| Concern                        | From-scratch AST                        | oxlint plugin                                                                                 |
| ------------------------------ | --------------------------------------- | --------------------------------------------------------------------------------------------- |
| Parser                         | We maintain our own SFC+TS pipeline     | oxlint already parses everything (oxc-parser)                                                 |
| Maintenance                    | We track Vue/Nuxt syntax forever        | Maintained by oxc team                                                                        |
| User opt-in                    | Only via CLI                            | Plugin is **standalone** — `vp lint` users opt in by adding to oxlint config (massive UX win) |
| Distribution                   | One channel (CLI)                       | Two channels (CLI + plugin)                                                                   |
| Existing rule reuse            | We rewrite `vue/no-mutating-props` etc. | oxlint's eslint-compat layer loads `eslint-plugin-vue` as-is                                  |
| Score parity with react-doctor | Different mechanism                     | Same mechanism                                                                                |

### Plugin distribution (the killer feature)

Users who already run `vp lint` can adopt our rules **without installing the CLI**:

```jsonc
// oxlintrc.json (or inline in vite.config.ts's lint block)
{
  "plugins": ["@geoql/oxlint-plugin-vue-doctor"],
  "extends": ["plugin:@geoql/vue-doctor/recommended"],
}
```

This is exactly how `oxlint-plugin-react-doctor` works. Your 11 personal Vue repos can adopt the plugin rules incrementally, **without ever installing the `vue-doctor` CLI**.

### What lives where

| Concern                                                   | Lives in                                                                |
| --------------------------------------------------------- | ----------------------------------------------------------------------- |
| AST visitors, rule logic                                  | `oxlint-plugin-*-doctor` packages                                       |
| Cross-file analysis (e.g., `deps/duplicate-vue-versions`) | `doctor-core` as a "post-lint check" (oxlint plugins are per-file)      |
| Score calculation                                         | `doctor-core`                                                           |
| Knip integration (dead-code)                              | `doctor-core`                                                           |
| Project capability detection (`vue:3.5`, `nuxt:4.4`)      | `doctor-core` → feeds `requires`/`disabledBy` to plugin's rule registry |
| Reporters                                                 | `doctor-core`                                                           |
| CLI flag parsing                                          | `vue-doctor` / `nuxt-doctor` packages (cac)                             |
| Programmatic API                                          | `doctor-core` (`runDoctor()`) re-exported from CLI packages             |

### Hybrid multi-pass engine (the implementation detail that shaped the design)

`doctor-core` does **not** run a single pass. Empirically (oxlint `1.67.0` + `@vue/compiler-sfc` spike): **oxlint JS plugins only receive the `<script>`/`<script setup>` ESTree of a `.vue` file — never the template AST.** So template-level checks (`v-for` missing `:key`, `v-if`+`v-for` precedence, inline object props in lists, …) cannot live in an oxlint plugin. The engine therefore runs multiple passes and merges their diagnostics:

1. **`runTemplatePass`** — `@vue/compiler-sfc.parse()` per `.vue`, walk the template AST, apply template rules in-process.
2. **`runSfcPass`** — SFC-level rules that need both `<script>` and `<script setup>` blocks (e.g. mixed Options/Composition API) — runs whenever the SFC descriptor parses.
3. **`runScriptPass`** — spawn `oxlint --format=json` as a subprocess with a generated `.oxlintrc.json` (`"plugins":["vue"]` for built-in Vue rules + `"jsPlugins":[…]` for `@geoql/oxlint-plugin-*-doctor`). Subprocess (not in-process) so doctor-core upgrades oxlint independently and never depends on internal Rust APIs (~50ms spin-up, acceptable for an audit tool).
4. **dead-code pass** (knip), **project/deps post-checks** (cross-file, e.g. `deps/duplicate-vue-versions`), and **cross-file pass** (Nuxt only).

All passes emit the canonical `Diagnostic` shape; `mergeDiagnostics` dedupes on `(file, line, column, ruleId)`, then the √-decay scorer (§7) produces the 0–100 score. This hybrid design is locked; the template/script split is the reason the rule set is partitioned across `doctor-core` (template + SFC + project) and the oxlint plugins (script).

---

## 11. Phased rollout

| Phase | Scope | Outcome |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------ |
| **0 — Scaffold** | Monorepo + 5 packages with placeholder `0.0.1`; vite-plus configs; husky; CI pipeline.yml verbatim from v-maplibre; release-please configured for all 5 packages; JSR publish wired | All 5 packages publish to npm + JSR as `0.0.1` empty placeholders |
| **1 — Vue MVP plugin** | `@geoql/oxlint-plugin-vue-doctor` with **5 hand-written rules** (the AI-priority-high ones): `no-destructure-props-without-toRefs`, `no-destructure-reactive-without-toRefs`, `no-non-null-assertion-on-ref-value`, `no-mixed-options-and-composition-api`, `no-imports-from-vue-when-auto-imported`. Plugin codegen (`pnpm gen`). Vitest fixtures. **Plugin is independently installable.** | `@geoql/oxlint-plugin-vue-doctor@0.1.0-alpha` consumable via `vp lint` |
| **2 — doctor-core + CLI** | `doctor-core` (oxlint runner, knip integration, score, agent reporter); `vue-doctor` CLI with `--preset`, `--format agent                                                                                                                                                                                                                                                                    | pretty                                                                 | json`, `--diff`, `--staged`, `--threshold`, `--fail-on`, `--explain` | `@geoql/vue-doctor@0.1.0-alpha` works on real Vue apps |
| **3 — Vue completeness** | Expand plugin to ~20 doctor-owned rules. Wire eslint-plugin-vue compat. SARIF + GitHub annotations reporters. `init`/`list-rules`/`explain`/`inspect` subcommands | `@geoql/vue-doctor@0.1.0` stable |
| **4 — Nuxt (plugin + CLI)** | `@geoql/oxlint-plugin-nuxt-doctor` (~25 Nuxt-4-only rules) + `@geoql/nuxt-doctor` CLI. `@nuxt/eslint` compat wired. Cloudflare-specific rule set | `@geoql/nuxt-doctor@0.1.0` |
| **5 — CI/Action** | Reusable GH Actions (`@geoql/vue-doctor-action@v1`, `@geoql/nuxt-doctor-action@v1`) in `.github/actions/`. SARIF upload to Code Scanning. PR sticky comments. Marketplace listings | Actions used by all 11 of your personal Vue repos |
| **6 — Polish** | Safe `--fix` auto-fixer (rule-by-rule opt-in). HTML report. Docs site (Nuxt 4 Content). Benchmark vs eslint-plugin-vue + @nuxt/eslint. `--no-respect-inline-disables` audit mode | `@geoql/{vue,nuxt}-doctor@1.0.0` |

---

## 12. Open risks / TBDs

| Risk                                                                                                                             | Severity              | Mitigation                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------------------------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **oxlint JS plugin API is ALPHA** (released 2026-03-11). API may break between minor versions before stabilizing                 | High                  | Pin `oxlint` to exact patch range (`~1.67.0`) in plugin packages' `peerDependencies`. Monitor [oxc releases](https://github.com/oxc-project/oxc/releases) weekly. react-doctor has shipped on this in production since `oxlint^1.66` — proves it's usable |
| **`c12@^4.0.0-beta.5` is beta** — config loader churn                                                                            | Medium                | Use `^3.x` as fallback if 4.x churns. c12 is unjs / Nuxt-team-maintained — they don't break casually                                                                                                                                                      |
| **eslint-plugin-vue compat in oxlint** — not every plugin rule is supported by oxlint's compat layer; some require ESLint engine | Medium                | Phase 3 explicit task: enumerate which `vue/*` rules work via oxlint compat. Fall back to running ESLint alongside oxlint for unsupported rules (slower but correct)                                                                                      |
| **`vp pack` for CLI binaries** — vite-plus is library-focused; need to verify `bin` shebang stays intact through bundling        | Medium                | Test in Phase 0 scaffold. Fallback: post-build script prepends shebang                                                                                                                                                                                    |
| **JSR publishing of `bin` packages** — JSR's `bin` field handling not yet documented as production-ready                         | Low                   | Phase 0 test. Worst case: publish CLI to npm only, plugins to both                                                                                                                                                                                        |
| **knip programmatic API stability** — knip 6.x is recent                                                                         | Low                   | Fallback: shell out to `knip --reporter json`                                                                                                                                                                                                             |
| **`pnpm dlx` / `npx` caching** — `npx -y` may serve stale versions                                                               | Low                   | Document `npx -y @geoql/vue-doctor@latest` pattern in README                                                                                                                                                                                              |
| **Nuxt 5 on the horizon** (`future.compatibilityVersion: 5`) — Vite Environment API migration, normalized page names             | Low (forward-looking) | Doctor targets Nuxt 4 stable. When Nuxt 5 lands, add `nuxt-5` capability + version-gate Nuxt-4-only rules with `disabledBy: ['nuxt:5']`. Architecture supports this natively (mirrors react-doctor's `requires`/`disabledBy` token system)                |
| **No `oxlint-plugin-vue-best-practices` upstream yet** — we're the first                                                         | Low/Opportunity       | Once stable, propose upstream into oxc org. The Vue ecosystem doesn't have a Vue equivalent to `eslint-plugin-react-hooks` — this is a real gap doctor fills                                                                                              |

---

## 13. Success criteria

v0.1 ships when:

- `npx -y @geoql/vue-doctor` runs on any of your 11 personal Vue repos in < 5s
- Score is deterministic (same input → same score, ±0)
- LLM (Claude) can read the `agent` output and produce a correct fix for ≥80% of findings without further context
- CI green on the doctor's own repo
- Published to npm + JSR

v1.0 ships when:

- Both tools have ≥30 / ≥50 rules respectively
- GH Action used by at least one of your personal repos in CI
- Docs site live at `doctor.geoql.dev` or similar

---

## 14. Coverage matrix — who owns what

For each known Vue/Nuxt concern, who's responsible? This decides "build it ourselves" vs "wire it up".

### Vue 3

| Concern                         | Existing tool                                           | Doctor's role                                                  |
| ------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------- |
| Syntax errors in `<template>`   | `vue/no-parsing-error` (eslint-plugin-vue)              | Wire it up                                                     |
| Mutating props                  | `vue/no-mutating-props` (epv)                           | Wire it up                                                     |
| Missing `:key`                  | `vue/require-v-for-key` (epv)                           | Wire it up                                                     |
| `v-if` + `v-for`                | `vue/no-use-v-if-with-v-for` (epv)                      | Wire it up                                                     |
| Reactivity destructure traps    | _(none)_                                                | **doctor-owned** (high AI-priority)                            |
| `shallowRef` for large data     | _(none, only docs guidance)_                            | **doctor-owned** (heuristic)                                   |
| Watch cleanup                   | _(none)_                                                | **doctor-owned**                                               |
| `v-memo` opportunities          | _(none)_                                                | **doctor-owned** (heuristic)                                   |
| Cross-file deps (duplicate Vue) | _(none)_                                                | **doctor-owned** (cross-file)                                  |
| Dead code                       | knip                                                    | Wire it up                                                     |
| Bundle size                     | vp / rolldown analyzer                                  | Advisory link                                                  |
| A11y                            | `eslint-plugin-vuejs-accessibility` (not in oxlint yet) | Advisory link                                                  |
| Type checking                   | `vue-tsc`                                               | Doctor flags presence of vue-tsc in devDeps; doesn't replicate |
| Outdated deps                   | `npm outdated` / `taze`                                 | Doctor's `deps/*` rules wrap npm registry                      |
| Code formatting                 | oxfmt                                                   | Out of scope — different tool                                  |

### Nuxt 4

| Concern                                  | Existing tool                                    | Doctor's role                                  |
| ---------------------------------------- | ------------------------------------------------ | ---------------------------------------------- |
| `import.meta.client` vs `process.client` | `@nuxt/eslint` has rule                          | Bump severity to error                         |
| Single root in `<template>`              | `@nuxt/eslint`'s `vue/no-multiple-template-root` | Wire it up                                     |
| CJS in nuxt.config                       | `@nuxt/eslint`                                   | Wire it up                                     |
| Auto-import collisions                   | _(none)_                                         | **doctor-owned**                               |
| `useFetch` vs raw `fetch`                | _(none)_                                         | **doctor-owned**                               |
| `useAsyncData` key safety                | _(none)_                                         | **doctor-owned** (cross-file)                  |
| Server route validation                  | _(none)_                                         | **doctor-owned**                               |
| SEO meta on pages                        | `@nuxtjs/seo` module (runtime)                   | Doctor flags missing `useSeoMeta` at lint-time |
| OG image setup                           | `@nuxtjs/seo` module                             | Advisory + Cloudflare-specific rule            |
| `compatibilityDate`                      | _(none)_                                         | **doctor-owned** (config check)                |
| Nuxt 4-incompatible modules              | _(none)_                                         | **doctor-owned** (curated list)                |
| Cloudflare deploy preset                 | _(none)_                                         | **doctor-owned**                               |
| `node:fs` in CF Workers                  | _(none)_                                         | **doctor-owned**                               |

---

## 15. Methodology — how this spec was cross-referenced

To prevent reinventing what already exists, the rule set was derived from these sources (captured 2026-05-28):

| Source                                                                                 | What we used it for                                                   | URL                                                     |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------- |
| react-doctor source (cloned to `/tmp/`)                                                | Architecture pattern, `defineRule` API, CLI flag set, score algorithm | https://github.com/millionco/react-doctor               |
| eslint-plugin-vue rule index                                                           | Comprehensive list of 252 existing Vue rules to AVOID duplicating     | https://eslint.vuejs.org/rules/                         |
| `@nuxt/eslint` source                                                                  | Default Nuxt rule set                                                 | https://github.com/nuxt/eslint                          |
| oxlint JS plugin docs (alpha)                                                          | Plugin API surface, framework gating model                            | https://oxc.rs/blog/2026-03-11-oxlint-js-plugins-alpha  |
| Nuxt 4 docs                                                                            | Migration breaking changes, h3 v2 patterns, `app/` directory defaults | https://nuxt.com/docs/4.x/                              |
| Vue 3 docs — style guide A/B/C/D priorities                                            | Anti-pattern severity calibration                                     | https://vuejs.org/style-guide/                          |
| Vue 3 performance guide                                                                | Performance-class rule sourcing                                       | https://vuejs.org/guide/best-practices/performance.html |
| Project skills: `vue-best-practices`, `nuxt-best-practices`, `nuxt-seo-best-practices` | AI-agent anti-pattern catalog (high-priority MVP rules)               | local                                                   |
| `npm view` for every dep                                                               | Version pin verification                                              | live                                                    |

**Re-audit cadence**: This methodology section MUST be re-run when:

- Vue 3.6 / Vue 4 / Nuxt 5 GA
- oxlint stabilizes JS plugin API (alpha → stable)
- eslint-plugin-vue adds new categories to its recommended preset
- @nuxt/eslint major version bump
